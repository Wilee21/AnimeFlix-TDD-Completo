# AnimeFlix TDD

Proyecto académico basado en una plataforma de anime.

## GitHub
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin URL_DEL_REPOSITORIO
git push -u origin main

## Ejecutar
mvn clean test
mvn jacoco:report
